<img src="../public/assets/img/burger.jpg">
<h1>Eat-Da-Burger!</h1>

<h2>Burgers to be Devoured!</h2>

<ul>
    {{#each burgers}}
    {{#unless devoured}}
    {{burger_name}}
    <button class="change-devour" data-id="{{id}}">
        DEVOUR
    </button>
    {{/unless}}
    {{/each}}
</ul>

<h2>Burgers Eaten!</h2>

<ul>
    {{#each burgers}}
    {{#if devoured}}
    {{burger_name}}
    {{/if}}
    {{/each}}
</ul>

<h2>Add a Burger</h2>
<form class="create-form">

    <div class="form-group">
        <label for="burger">Burger Name:</label><br>
        <input type="text" id="burger" name="name">
    </div>

    <button type="submit">Add Burger</button>
</form>